import { DocsClient } from '@/components/pages/DocsClient'

export const metadata = {
  title: 'Docs — Planckspace',
  description:
    'Install Planckspace in 60 seconds. Quickstart, architecture and the privacy model.',
}

export default function DocsPage() {
  return <DocsClient />
}
